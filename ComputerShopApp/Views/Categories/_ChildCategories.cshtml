@using ComputerShopDomainLibrary
@model IEnumerable<Category>
@*
    For more information on enabling MVC for empty projects, visit http://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    SelectList parentCategories = new SelectList(Model, nameof(Category.Id), nameof(Category.Name));
}
<div class="form-group">
    <label for="parentCategoryId" class="control-label"></label>
    <select name="parentCategoryId[]" class="form-control" asp-items="@parentCategories">
        <option value="@(null)">Nothing</option>
    </select>
</div>